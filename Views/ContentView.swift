import SwiftUI

struct ContentView: View {
    var body: some View {
        ZStack {
            Color("old-book")
            NavigationStack {
                VStack {
                    
                }
            }
        }
    }
}

#Preview {
    ContentView()
}
